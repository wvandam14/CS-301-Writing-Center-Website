<?php

// AUTH.PHP

	// require_once("db_connection.php");

	// $pagename = basename($_SERVER['PHP_SELF']);

	// if(empty($_SESSION['permission']) || !pageAuth($pagename)) { // don't allow the user to see this page!
 //        header('location:login.php');
 //    }

	
?>